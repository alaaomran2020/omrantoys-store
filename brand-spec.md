# هوية شركة عمران التجارية

## بيانات العلامة

- الاسم التجاري: شركة عمران التجارية
- العلامة الرقمية: عمران للألعاب — Omran Toys
- الوصف: لعب أطفال ومستلزمات حفلات
- السوق: جمهورية مصر العربية
- قناة الطلب: WhatsApp Business `01555570269`

---

## الشعار (Logo)

### الملفات المعتمدة

| المسار | الاستخدام |
|---|---|
| `public/brand/logo.svg` | المصدر المتجهي (الأفضل للطباعة والتكبير) |
| `public/brand/logo.png` | **ملف العرض الرسمي** — يُستخدم في الهيدر والفوتر وكل واجهات الموقع |
| `public/brand/logo-256.png` | نسخة مصغّرة للاستخدامات الخفيفة |
| `public/brand/favicon.png` / `favicon-32.png` | أيقونة المتصفح |
| `public/brand/apple-touch-icon.png` | أيقونة إضافة الموقع للشاشة الرئيسية (180×180) |
| `public/manifest.webmanifest` | بيانات التطبيق (PWA) للتثبيت على الموبايل |

### قواعد الاستخدام

- **المساحة الآمنة**: مسافة بيضاء حول الشعار لا تقل عن 25% من ارتفاعه من كل جانب.
- **الحد الأدنى للحجم**: 40 بكسل ارتفاعاً على الشاشة، و20 مم في الطباعة.
- يُعرض الشعار على خلفية بيضاء أو فاتحة (كريمي `#FFFDF8`) أو كحلي `#10152F`.
- **ممنوع**: تغيير النسب، تدوير الشعار، إضافة ظل أو تدرج فوقه، تغيير الألوان، أو وضعه على خلفية مزدحمة تحجب التفاصيل.
- في الهيدر يظهر **الشعار فقط بدون اسم نصي** — الحاوية مرنة (`h-12 sm:h-14` مع `object-contain`) فتناسب الشعار المربع أو الممتد بنفس الكود.

### تحديث ملف الشعار

لاستبدال الشعار: ضع الملف الجديد باسم `public/brand/logo.png` (يفضل PNG بخلفية شفافة، مربع أو ممتد، بدقة 512 بكسل على الأقل).
لا يحتاج الأمر أي تعديل في الكود — جميع الواجهات تقرأ من نفس المسار. لتحديث الأيقونات المشتقة:

```bash
node -e "
const sharp=require('sharp');const fs=require('fs');const img=fs.readFileSync('public/brand/logo.png');
sharp(img).resize(256,256,{fit:'contain',background:'#00000000'}).png().toFile('public/brand/logo-256.png');
sharp(img).resize(180,180,{fit:'contain',background:'#00000000'}).png().toFile('public/brand/apple-touch-icon.png');
sharp(img).resize(64,64,{fit:'contain',background:'#00000000'}).png().toFile('public/brand/favicon.png');
sharp(img).resize(32,32,{fit:'contain',background:'#00000000'}).png().toFile('public/brand/favicon-32.png');
"
```

---

## لوحة الألوان

| Token | Hex | Tailwind | الاستخدام |
|---|---|---|---|
| Coral | `#FF4D6D` | `toy-red` | الأزرار الرئيسية، الأسعار، دعوات الطلب |
| Golden | `#FFC017` | `toy-yellow` | العروض، الشارات، اللمسات المرحة |
| Turquoise | `#16A6B6` | `toy-blue` | الشحن، الروابط، مؤشرات الخدمة |
| Mint | `#06D6A0` | `toy-green` | التوفر، النجاح، التأكيدات |
| Purple | `#8338EC` | `toy-purple` | لمسات مرحة وتدرجات ثانوية |
| Orange | `#FF7B00` | `toy-orange` | التخفيضات |
| Navy | `#10152F` | `toy-navy` | العناوين الداكنة، الفوتر، النصوص الأساسية |
| Ink | `#2B2D42` | `toy-ink` | نص ثانوي داكن |
| Warm White | `#FFFDF8` | `toy-cream` | خلفيات الصفحة ومساحات المنتجات |

الظلال المعتمدة: `shadow-soft` للبطاقات، `shadow-soft-lg` عند التحويم، `shadow-pop` للأيقونات والأزرار.

---

## الخطوط

- **العناوين**: `Baloo Bhaijaan 2` (مستدير ومرح، يدعم العربية) — يُطبّق تلقائياً على `h1–h4` والكلاس `.font-display`.
- **الواجهة والنصوص**: `Cairo`، مع `Tajawal` كبديل.
- **النبرة**: ودودة، واضحة، مطمئنة للعائلة، وبعيدة عن المبالغة.

---

## العناصر الجاهزة (Design Components)

| الكلاس | الوظيفة |
|---|---|
| `.brand-card` | بطاقة بيضاء بحواف `2rem` وظل ناعم |
| `.btn-playful` | زر مرح بحواف دائرية وحركة ضغط عند اللمس |
| `.badge-playful` | شارة دائرية صغيرة للعروض والحالات |
| `.bg-confetti` | خلفية نقاط ملونة خفيفة (تُستخدم في الهيرو) |
| `.brand-ring` | إطار أبيض ناعم حول الشعار في الهيدر |

---

## قواعد التحرير

- اسم الشركة في المواضع المؤسسية: «شركة عمران التجارية».
- اسم المتجر الرقمي: «عمران للألعاب».
- كل الأسعار بالجنيه المصري (ج.م) وشاملة ضريبة القيمة المضافة.
- إتمام الطلبات يتم بالتأكيد عبر WhatsApp Business.
